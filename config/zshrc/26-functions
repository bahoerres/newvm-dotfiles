# -----------------------------------------------------
# FUNCTIONS
# -----------------------------------------------------
# Tree view with configurable depth
lt() {
  eza -a --tree --level="${1:-1}" --icons=always
}
